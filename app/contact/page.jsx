import Transition from "@/components/utils/Transition"

export const metadata = { 
  title: "Contact",
}

const Contact = () => {
  return (
    <Transition>
      <div>Contact</div>
    </Transition>
  )
}

export default Contact